apiVersion: v1
kind: Service
metadata:
  name: redis
spec:
  ports:
    - port: {{ .Values.workerConfig.containerPort}}
      targetPort: {{ .Values.workerConfig.containerPort}}
  selector:
    app: redis
